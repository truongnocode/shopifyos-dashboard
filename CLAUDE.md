# ShopifyOS Dashboard

## Git workflow
- After committing changes, always automatically push to GitHub (`git push`) without asking for confirmation.
- After pushing, rebuild dist if needed: `npm run build`.
